

# This manifest file contains the configuration for the payment application, including a ConfigMap for database configuration, a Deployment for the application, and a Service to expose it.

apiVersion: v1
kind: ConfigMap
metadata:
  name: db-config
  namespace: roboshop
data:
  MONGO_URL: "mongodb://mongodb-service:27017/payment"
  MONGO_DB: "true"

---

apiVersion: v1
kind: Namespace
metadata:
  name: payment   #why different namespace for each application? to isolate the applications and to manage the resources of each application separately
  labels:
    project: roboshop
    environment: dev
    purpose: poc

---

#deployment for payment application

apiVersion: apps/v1
kind: Deployment
metadata:
  name: payment-deployment
  namespace: roboshop
  labels:
    app: payment
    project: roboshop
    environment: dev
    purpose: poc

  annotations:
    kubernetes.io/change-cause: "v1 payment deployment"

    # the namespace in which the deployment will be created 
spec:
  replicas: 1
  selector:
    matchLabels:
      
      app: payment
      project: roboshop
      environment: dev
      purpose: poc

# the pod template

  template:
    metadata:
      labels:
        app: payment
        project: roboshop
        environment: dev
        purpose: poc
    spec:
      containers:
      - name: payment
        image: sandeep0109/payment:2.2.2

# always pull the image to ensure we get the latest version of the application

        imagePullPolicy: Always
        ports:
        - containerPort: 27017

# resource limits and requests for the container

        resources:
          limits:
            memory: "128Mi"
            cpu: "500m"
          requests:
            memory: "64Mi"
            cpu: "250m"

# pass the environment variables to the application using configmap

        envFrom:
        - configMapRef:
            name: db-config


#check the health of the application using readiness probe
        

        readinessProbe:
          httpGet:
            path: /health
            port: 8080
          failureThreshold: 3
          initialDelaySeconds: 15
          tcpSocket:
            port: 8080


#check the health of the application using liveness probe

        livenessProbe:
          exec:
            command:
              - /tmp/healthy
          periodSeconds: 10
          failureThreshold: 3
        

---

#nodeport service


apiVersion: v1
kind: Service
metadata:
  name: payment-service
  namespace: roboshop
  labels:
    app: payment
    project: roboshop
    environment: dev
    purpose: poc

spec:
  type: LoadBalancer
  selector:
    app: payment
    project: roboshop
    environment: dev
    purpose: poc 
  ports:
  - protocol: TCP
    port: 8080  #service port
    targetPort: 80 #container port